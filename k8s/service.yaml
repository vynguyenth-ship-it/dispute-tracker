apiVersion: v1
kind: Service
metadata:
  name: gmail-classifier
  namespace: dispute-tools
spec:
  selector:
    app: gmail-classifier
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
